apiVersion: apps/v1
kind: Deployment
metadata:
  name: task2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task2
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: task2
    spec:
      containers:
        - name: task2-app
          image: ghcr.io/${{ secrets.GIT_USER }}/$IMAGE:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 300m
            limits:
              cpu: 300m
---
apiVersion: v1
kind: Service
metadata:
  name: task2-service
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: task2